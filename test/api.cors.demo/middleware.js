const cookie = {
  bouncer: require('../../api.cors.demo/middleware/cookieSessionBouncer'),
  loader: require('../../api.cors.demo/middleware/cookieSessionLoader')
}

describe('#cookieSessionBouncer', function () {
  it.skip('Should have tests')
})

describe('#cookieSessionLoader', function () {
  it.skip('Should have tests')
})
